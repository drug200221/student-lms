export interface IConfiguration {
  id: number | null,
  testId: number,
  countAttempt: number | null,
  isDisplayAllQuestions: boolean,
  isVisibleResult: boolean,
  isRandomQuestions: boolean,
  countQuestions: number | null
}
